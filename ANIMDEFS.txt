// RS_HandHUD -- one canvas per mount, named as the Skin of that mount's
// MODELDEF block and painted by RS_HandHUDCanvas.
//
// 200x200 IS NOT A ROUND NUMBER, IT IS THE DIAL'S NUMBER. w_dial.md3 is UV'd
// for watchface.png, which is 200x200 square. A 128x64 canvas on that mesh is
// stretched two to one across a round face and everything on it lands in the
// wrong place -- so the canvas matches the art it replaces, exactly.
//
// MUST MATCH RS_HandHUDCanvas.W/H.
canvastexture RSHUDMAIN 200 200 fit 200 200 WorldPanning
canvastexture RSHUDOFF  200 200 fit 200 200 WorldPanning
canvastexture RSHUDGUNM 300 300 fit 300 300 WorldPanning
canvastexture RSHUDGUNO 300 300 fit 300 300 WorldPanning
